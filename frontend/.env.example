# URL del backend API (fallback si leader-resolver falla)
VITE_API_URL=http://localhost:8000/api/v1/

# URL del servicio Leader Resolver
VITE_LEADER_RESOLVER_URL=http://localhost:3001

# Usar Leader Resolver para descubrir el líder dinámicamente (true por defecto)
# Establecer a "false" para usar solo VITE_API_URL
VITE_USE_LEADER_RESOLVER=true