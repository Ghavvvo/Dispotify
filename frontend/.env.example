# Frontend - Variables de entorno para Vite
# URL del Leader Proxy (apunta al proxy que redirige al líder automáticamente)
VITE_API_URL=http://localhost:3001/api/v1/

# URL para archivos estáticos (también a través del proxy)
VITE_STATIC_URL=http://localhost:3001

# Leader Proxy - Variables de entorno para el proxy integrado
# Nombre DNS/alias de red de los backends en Docker Swarm
CLUSTER_DNS_NAME=dispotify-cluster

# Puerto interno donde escuchan los backends
BACKEND_PORT=8000

# Endpoint que devuelve información del líder
LEADER_ENDPOINT=/cluster/leader

# Puerto donde escucha el proxy
SERVICE_PORT=3001

# TTL del caché del líder en milisegundos
LEADER_CACHE_TTL=5000
